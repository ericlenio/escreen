# vim:filetype=sh
l() {
  local os=$(uname -s)
  case $os in
    Darwin|FreeBSD|OpenBSD)
      ls -laF "$@"
      ;;
    *)
      ls -laF --time-style=+'%D %H:%M:%S' "$@"
      ;;
  esac
}
export -f l

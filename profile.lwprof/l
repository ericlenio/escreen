# vim:filetype=sh
function l() {
  local os=$(uname -s)
  case $os in
    Darwin|FreeBSD)
      ls -laF "$@"
      ;;
    *)
      ls -laF --time-style=+'%D %H:%M:%S' "$@"
      ;;
  esac
}
export -f l

# vim:filetype=sh
function _ES_source_script() {
  local script=$1
  if [ ! -e "$script" ]; then
    echo "No such file"
    return
  fi
  local c
  c="$(openssl enc -a -d -aes256 -in $script)" && eval "$c"
}
export -f _ES_source_script
